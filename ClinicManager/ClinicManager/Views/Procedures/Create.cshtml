@model ProcedureOptionDto
@{
    ViewData["Title"] = "Dodaj procedurę do katalogu";
}

<h2>Dodaj procedurę do katalogu</h2>

<form asp-action="Create" method="post" class="row g-3">
    @Html.AntiForgeryToken()
    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

    <div class="col-md-8">
        <label asp-for="Name" class="form-label">Nazwa procedury *</label>
        <input asp-for="Name" class="form-control" placeholder='np. "USG jamy brzusznej"' />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <div class="col-md-4">
        <label asp-for="Cost" class="form-label">Koszt świadczenia (PLN) *</label>
        <input asp-for="Cost" type="number" step="0.01" class="form-control" placeholder='np. "150.00"' />
        <span asp-validation-for="Cost" class="text-danger"></span>
    </div>

    <div class="col-md-12">
        <label asp-for="Description" class="form-label">Opis procedury</label>
        <textarea asp-for="Description" class="form-control" rows="4" placeholder="Wprowadź krótki opis przebiegu procedury medycznej..."></textarea>
        <span asp-validation-for="Description" class="text-danger"></span>
    </div>

    <div class="col-12 d-flex gap-2">
        <button type="submit" class="btn btn-primary">Zapisz</button>
        <a asp-action="Index" class="btn btn-secondary">Anuluj</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
